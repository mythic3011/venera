# AppDbHelper Write Audit

Date: 2026-05-05

| file                                                      | line/function                                                                                                                                                          | statement kind                                 | normal runtime write?     | action                                                                                                                                       |
| --------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------- | ------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------- |
| `lib/foundation/appdata.dart`                             | `_saveToDb`                                                                                                                                                            | `transaction` + `customStatement`              | yes                       | routed through `AppDbHelper.instance.transaction('appdata.save', ...)`                                                                       |
| `lib/features/reader/data/reader_session_repository.dart` | `saveProgress`, `updateActiveTab`, `upsertSession`                                                                                                                     | write orchestration                            | yes                       | routed via store methods that run through `AppDbHelper`                                                                                      |
| `lib/foundation/db/unified_comics_store.dart`             | `saveReaderProgress`                                                                                                                                                   | `transaction` + `customStatement`              | yes                       | routed through `AppDbHelper.instance.transaction('reader_sessions.saveProgress', ...)`                                                       |
| `lib/foundation/db/unified_comics_store.dart`             | `_runReaderSessionUpsert`, `upsertReaderTab`, `deleteReaderSession`, `deleteReaderTab`, `deleteReaderActivity`, `clearReaderActivity`                                  | `runCanonicalWrite` + `customWrite` + `DELETE` | yes                       | routed via `runCanonicalWrite`/`AppDbHelper.instance.customWrite(...)` (Phase 5.2 reader session/tab lane completed)                         |
| `lib/foundation/db/unified_comics_store.dart`             | `upsertSourceRepository`, `deleteSourceRepository`                                                                                                                     | `customStatement`                              | yes                       | routed through `AppDbHelper.instance.customWrite(...)`                                                                                       |
| `lib/foundation/db/unified_comics_store.dart`             | `replaceSourcePackagesForRepository`                                                                                                                                   | `.transaction(...)` + inner writes             | yes                       | routed through `AppDbHelper.instance.transaction(...)` + `customWrite(...)`                                                                  |
| `lib/foundation/db/unified_comics_store.dart`             | `seedDefaultSourcePlatforms`, `upsertSourcePlatform`, `upsertSourcePlatformAlias`                                                                                      | `.transaction(...)` + `customStatement`        | yes                       | routed through `AppDbHelper.instance.transaction(...)` / `customWrite(...)`                                                                  |
| `lib/foundation/db/unified_comics_store.dart`             | `upsertComicSourceLink`, `upsertChapterSourceLink`, `upsertPageSourceLink`, `upsertSourceTag`, `attachSourceTagToComicSourceLink`, `clearSourceTagsForComicSourceLink` | transaction/customStatement                    | yes                       | routed through `AppDbHelper.instance.transaction(...)` / `customWrite(...)`                                                                  |
| `lib/foundation/db/unified_comics_store.dart`             | `replaceEhTagTaxonomyRecords`                                                                                                                                          | `.transaction(...)` + inner writes             | yes                       | routed through `AppDbHelper.instance.transaction(...)` + inner `customWrite(...)` (single atomic replace preserved)                          |
| `lib/foundation/db/unified_comics_store.dart`             | `upsertFavoriteFolder`, `deleteFavoriteFolder`, `upsertFavoriteFolderItem`, `deleteFavoriteFolderItem`, `deleteFavoriteFolderItemsByComic`                             | `customStatement`                              | yes                       | routed through `AppDbHelper.instance.customWrite(...)`                                                                                       |
| `lib/foundation/db/unified_comics_store.dart`             | `renameFavoriteFolder`, `replaceFavoriteFolderOrder`                                                                                                                   | `.transaction(...)` + inner writes             | yes                       | routed through `AppDbHelper.instance.transaction(...)` + `customWrite(...)`                                                                  |
| `lib/foundation/db/unified_comics_store.dart`             | `upsertPageOrder`                                                                                                                                                      | `customStatement`                              | yes                       | routed through `AppDbHelper.instance.customWrite('page_order.upsert', ...)`                                                                  |
| `lib/foundation/db/unified_comics_store.dart`             | `replacePageOrderItems`                                                                                                                                                | `.transaction(...)` + inner writes             | yes                       | routed through `AppDbHelper.instance.transaction('page_order.replace_items', ...)` + inner `customWrite(...)` (single atomic unit preserved) |
| `lib/foundation/db/unified_comics_store.dart`             | `deleteComicTitlesForComic`, `deleteChaptersForComic`, `deletePagesForChapter`                                                                                         | `customStatement`                              | yes                       | routed through `AppDbHelper.instance.customWrite(...)` (Phase 5.1 cleanup/delete cascade lane)                                               |
| `lib/foundation/db/unified_comics_store.dart`             | `upsertChapter`, `upsertPage`                                                                                                                                          | `customStatement`                              | yes                       | routed through `AppDbHelper.instance.customWrite(...)` (Phase 5.3 chapter/page metadata lane completed; `chapter_page.` labels)              |
| `lib/foundation/db/history_store.dart`                    | `init`, `upsert*`, `delete*`, `clearHistory`                                                                                                                           | `customStatement`                              | yes (legacy runtime path) | routed through `AppDbHelper.instance.customWrite(...)`                                                                                       |
| `lib/foundation/db/history_store.dart`                    | `batchDeleteHistories`                                                                                                                                                 | `.transaction(...)` + inner `customStatement`  | yes (legacy runtime path) | routed through `AppDbHelper.instance.transaction(...)`; inner calls intentionally not double-wrapped                                         |
| `lib/foundation/db/local_comics_store.dart`               | `init`, `upsert`, `delete*`, `update*`                                                                                                                                 | `customStatement`                              | yes (legacy runtime path) | routed through `AppDbHelper.instance.customWrite(...)`                                                                                       |
| `lib/foundation/db/local_comics_store.dart`               | `batchDelete`                                                                                                                                                          | `.transaction(...)` + inner `customStatement`  | yes (legacy runtime path) | routed through `AppDbHelper.instance.transaction(...)`; inner calls intentionally not double-wrapped                                         |
| `lib/foundation/db/unified_comics_store/schema.dart`      | schema bootstrap helpers                                                                                                                                               | `customStatement`                              | no                        | leave as migration/setup                                                                                                                     |
| `lib/foundation/db/unified_comics_store/migrations.dart`  | migration helpers                                                                                                                                                      | `customStatement`                              | no                        | leave as migration                                                                                                                           |
| `lib/foundation/db/unified_comics_store.dart`             | `beforeOpen` PRAGMA setup                                                                                                                                              | `customStatement`                              | no                        | leave as setup                                                                                                                               |
| `lib/foundation/db/unified_comics_store.dart`             | remaining direct `customStatement` usage                                                                                                                               | setup plus AppDbHelper-controlled inner writes | no uncontrolled normal runtime write | runtime writes centralized through `AppDbHelper`; setup paths and helper-controlled inner statements excluded by design                     |
| `lib/foundation/db/*`                                     | all `customSelect` paths                                                                                                                                               | `customSelect`                                 | no                        | leave as read-only                                                                                                                           |

Notes:

- Nested writes were not double-wrapped when already inside `AppDbHelper.instance.transaction(...)`.
- This pass intentionally avoids broad SQL behavior changes and schema changes.

## Phase 2 completion note

- Phase 2 completed lanes are routed through `AppDbHelper` for write-side execution:
  - source package/source link lane
  - favorite folder lane
  - page order lane
  - EH tag taxonomy lane
- Remaining `unified_comics_store` runtime write paths outside these completed lanes are tracked as Phase 5+ debt.
- Phase 5.2 completed lane: reader session/tab legacy writes are routed through `runCanonicalWrite`/`AppDbHelper`.
- Phase 5.3 completed lane: chapter/page metadata writes are routed through `AppDbHelper` with `chapter_page.` label prefix.
- Phase 5.5 completed lane: comic metadata/library item writes are routed through `AppDbHelper` with `comic_library.` label prefix.
- This note does not cover setup paths or helper-controlled inner SQL statements; normal runtime write entrypoints in `unified_comics_store.dart` are now centralized through `AppDbHelper`.

## Phase 5 audit: previously remaining unified_comics_store runtime writes

Scope applied:

- Originally included direct runtime writes in `lib/foundation/db/unified_comics_store.dart` (`customStatement` calls and any direct `.transaction(...)` usage).
- Excluded schema/migration/setup (`beforeOpen`, migrations, bootstrap), and excluded read-only `customSelect`.
- No runtime behavior or SQL changed in this phase.

| method/function                                                                          | SQL operation type                                   | domain                                                              | normal runtime write                        | completed execution lane           | risk level | suggested test coverage                                                                               |
| ---------------------------------------------------------------------------------------- | ---------------------------------------------------- | ------------------------------------------------------------------- | ------------------------------------------- | ---------------------------------- | ---------- | ----------------------------------------------------------------------------------------------------- |
| `upsertCacheEntry`, `deleteCacheEntry`, `deleteAllCacheEntries`, `touchCacheEntryAccess` | `INSERT ... ON CONFLICT UPDATE`, `DELETE`, `UPDATE`  | cache entry lifecycle                                               | yes                                         | miscellaneous legacy writes        | medium     | unit tests for insert/update/delete/touch paths, expiry ordering integration test                     |
| `upsertAppSetting`, `clearAppSettings`                                                   | `INSERT ... ON CONFLICT UPDATE`, `DELETE`            | app settings KV (unified KV API, distinct from `Appdata._saveToDb`) | yes                                         | miscellaneous legacy writes        | low        | unit tests for upsert overwrite semantics and clear-all behavior                                      |
| `upsertSearchHistory`, `clearSearchHistory`                                              | `INSERT ... ON CONFLICT UPDATE`, `DELETE`            | search history                                                      | yes                                         | miscellaneous legacy writes        | low        | unit tests for position update/conflict resolution and full clear                                     |
| `upsertImplicitData`, `clearImplicitData`                                                | `INSERT ... ON CONFLICT UPDATE`, `DELETE`            | implicit app data                                                   | yes                                         | miscellaneous legacy writes        | low        | unit tests for idempotent upsert and clear                                                            |
| `upsertHistoryEvent`                                                                     | `INSERT ... ON CONFLICT UPDATE`                      | read history timeline                                               | yes                                         | miscellaneous legacy writes        | medium     | integration tests for repeated-progress upsert and timeline ordering                                  |
| direct Drift `.transaction(...)` in this file                                            | n/a                                                  | transaction bypass audit                                            | no remaining direct Drift transaction usage | n/a                                | low        | static check: keep `AppDbHelper.instance.transaction(...)` as the only runtime transaction entrypoint |

Notes:

- `saveReaderProgress` is already fully routed via helper transaction. Its rollback atomicity is now covered by reader session/tab lane regression tests.
- Phase 5.4 completed: remote match/import reconciliation writes (`upsertRemoteMatchCandidate`, `deleteRemoteMatchCandidate`) are now routed through `AppDbHelper.instance.customWrite(...)` with `remote_match.` labels and no SQL semantic changes.
- Phase 5.5 completed: comic metadata/library item writes (`upsertComic`, `insertComicTitle`, `upsertFavorite`, `deleteFavorite`, `upsertLocalLibraryItem`, `deleteLocalLibraryItemById`, `upsertUserTag`, `attachUserTagToComic`, `removeUserTagFromComic`) are now routed through `AppDbHelper.instance.customWrite(...)` with `comic_library.` labels and no SQL semantic changes.
- Phase 5.6 completed: miscellaneous legacy writes (`upsertCacheEntry`, `deleteCacheEntry`, `deleteAllCacheEntries`, `touchCacheEntryAccess`, `upsertAppSetting`, `clearAppSettings`, `upsertSearchHistory`, `clearSearchHistory`, `upsertImplicitData`, `clearImplicitData`, `upsertHistoryEvent`) are now routed through `AppDbHelper.instance.customWrite(...)` with scoped labels (`cache.`, `settings_kv.`, `search_history.`, `implicit_data.`, `history_event.`) and no SQL semantic changes.
- Unified store runtime writes are now centralized through `AppDbHelper` (excluding setup/migration/read-only paths by design).

## Phase 5 follow-up: lane priority execution table (doc-only)

Completed Phase 5.1 cleanup/delete cascade writes, Phase 5.2 reader session/tab legacy writes, Phase 5.3 chapter/page metadata writes, Phase 5.4 remote match/import reconciliation writes, Phase 5.5 comic metadata/library item writes, and Phase 5.6 miscellaneous legacy writes are omitted from the table below.

No remaining runtime write lane in `unified_comics_store.dart` is pending centralization; remaining direct `customStatement` usage is constrained to setup/migration/read-only paths.

## Phase 6 final hardening note

- Normal runtime write entrypoints in `unified_comics_store.dart` are centralized through `AppDbHelper`.
- Remaining direct `customStatement` usage is limited to PRAGMA setup and helper-controlled inner SQL blocks.
- Direct Drift transaction bypass is guarded against.
- Future runtime writes must enter through `AppDbHelper` with approved scoped labels.
